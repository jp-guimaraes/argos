/target
**/*.rs.bk

# Manually downloaded test ISOs (backlog E9 / real-hardware verification) --
# never meant to be committed.
/.testdata/
.playwright-mcp/

# Session-local working notes (may contain credentials pasted during a session)
.claude/
HANDOFF.md
