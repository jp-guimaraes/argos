[package]
name = "argos-platform-windows"
description = "Stub Windows backend for PlatformOps. Not implemented -- exists only to prove the trait carries no Unix-specific assumptions. Out of scope until phase 2."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
argos-core = { workspace = true }
argos-platform = { workspace = true }
