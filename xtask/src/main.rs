//! Placeholder for developer tasks. Nothing here yet -- planned uses (backlog
//! E4.4, E9): generating small synthetic ISO fixtures for `argos-core` tests
//! that currently build their fixtures inline, and driving the loop-device /
//! `hdiutil` based integration tests once E9 lands.

fn main() {
    println!("xtask: no tasks defined yet");
}
