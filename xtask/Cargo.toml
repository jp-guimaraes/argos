[package]
name = "xtask"
description = "Developer-only tasks (test fixture generation, release helpers). Not part of the published crates."
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
# Only needed by the vendor-uefi-ntfs subcommand, to build the tiny FAT boot
# image committed under crates/argos-privileged/assets/ -- see that
# subcommand's doc comment. Never a dependency of anything actually shipped.
fatfs = "0.3"
